name: com.adg.dir-stats
protocol_version: 1
description: dir-stats on host integration example
os: linux

commands:
  dir-stats:
    command:
      - ./bin/dir-stats.sh
    interval: 30
    prefix: integration/custom/dir-stats