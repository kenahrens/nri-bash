name: com.adg.ssl-cert-expire
protocol_version: 1
description: ssl-cert-expire on host integration
os: linux

commands:
  ssl-cert-expire:
    command:
      - ./bin/ssl-cert-expire.sh
    interval: 30
    prefix: integration/custom/ssl-cert-expire